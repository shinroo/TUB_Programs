#include <stdio.h>

int main(int argc, char const *argv[])
{
	if (argc < 2)
	{
		printf("usage: bla bli blub\n");
	}
	if (argc == 2)
	{
		printf("nur parameterdatei uebergeben\n");
		printf("programmname: %s\n", argv[0]);
		const char *filename = argv[1];
		printf("filename: %s\n", filename);
	}
	return 0;
}