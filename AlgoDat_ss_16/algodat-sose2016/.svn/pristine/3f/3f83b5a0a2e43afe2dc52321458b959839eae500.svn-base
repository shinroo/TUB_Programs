import java.util.ArrayList;
import java.util.Iterator;

public class Tutorial {

	public static void main(String[] args) {
		Body b = new Tetrahedron(5);
		Tetrahedron t2 = (Tetrahedron) b;
		if (b instanceof Cuboid) {
			Cuboid i = (Cuboid) b;
		}
		Tetrahedron t = new Tetrahedron(5);
		// Body b2 = new Body();
		System.out.println(b.calculateVolume());
		System.out.println(b.calculateSurface());
		// b.m();
		// t.m();

		ArrayList<Integer> a = new ArrayList<Integer>();
		a.add(1);
		System.out.println(a.get(0));

		Iterator<Integer> it = a.iterator();
		while (it.hasNext()) {
			int k = it.next();
		}

		for (it = a.iterator(); it.hasNext();) {
			int k = it.next();
		}

		for (int k : a) {

		}

		int[] arr = { 1, 2, 3 };
		// arr = new int[5];

		for (int k : arr) {

		}

		for (int i = 0; i < arr.length; ++i) {
			int k = arr[i];
		}

		arr[0] = 4567;

		Picture p = new Picture(10, 5);
	}

}
