import java.util.ArrayList;
import java.util.Iterator;

public class Tutorial {

	public static void main(String[] args) {
//		Body b = new Tetrahedron();
		Tetrahedron t = new Tetrahedron(5);
//		Body b2 = new Body();
//		Tetrahedron t2 = new Body();
		System.out.println(t.calculateSurface());
		

		//int[] arr = new int[5];
		int[] arr = { 1, 2, 3, 4, 5 }; // short for arr[0] = 1; arr[1] = 2; ...
		int v = arr[1]; // read from index 1
		arr[1] = 4; // write to index 1
		
		for (int i = 0; i < arr.length; ++i) {
			int k = arr[i];
			// TODO
		}
		
		for (int k : arr) {
			// TODO
		}
		
		
		ArrayList<Integer> list = new ArrayList<Integer>();
		list.add(4); // add Element
		v = list.get(0);
		
		Iterator<Integer> it = list.iterator();
		while (it.hasNext()) {
			int j = it.next();
			// TODO
		}
		
		for (int j : list) {
			// TODO
		}
		
		int j;
		for (Iterator<Integer> it2 = list.iterator(); it2.hasNext();) {
			j = it2.next();
			// TODO
		}
	}
	
}
